import { fetchContent } from './service';

import { makeHtmlContent, compareVersion } from "../../../utils";
const state = {
}

const actions = {
}

const mutations = {
}

export default {
  namespaced: true,
  state,
  actions,
  mutations
}